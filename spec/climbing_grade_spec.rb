require "spec_helper"

RSpec.describe ClimbingGrade do
  it "has a version number" do
    expect(ClimbingGrade::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
